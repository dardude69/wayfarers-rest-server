const router = require('express').Router();

router.post('/', (req, res) => {
  // TODO: Create account.
});

router.post('/:username/movements', (req, res) => {
  // TODO
});

module.exports = router;
